[build-system]
requires = ["hatchling>=1.26"]
build-backend = "hatchling.build"

[project]
name = "maintainers-copilot-chatbot"
version = "0.1.0"
description = "Streamlit UI for Maintainers Copilot"
requires-python = ">=3.12"
dependencies = [
  "streamlit>=1.41",
  "httpx>=0.28",
]
